/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package io.github.kory33.guardedqueries.app;

import io.github.kory33.guardedqueries.list.LinkedList;

import static io.github.kory33.guardedqueries.utilities.StringUtils.join;
import static io.github.kory33.guardedqueries.utilities.StringUtils.split;
import static io.github.kory33.guardedqueries.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
